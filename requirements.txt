# ──────────────────────────────────────────────────────────────────
# Anomaly Detection System - Python Dependencies
# ──────────────────────────────────────────────────────────────────
# Only packages directly imported by the codebase are listed here.
# Transitive dependencies (e.g. scipy via scikit-learn) are resolved
# automatically by pip.
#
# vLLM is an external server process, not a Python library dependency.
# Deploy it separately: vllm serve <model>
# ──────────────────────────────────────────────────────────────────

# ── Core API Framework ───────────────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0
websockets>=14.0
httpx>=0.27.0
pydantic>=2.0

# ── Data Processing & ML ────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
pyod>=1.1,<2              # ECOD and Extended IForest (Wave 1)

# ── Deep Learning ───────────────────────────────────────────────
torch>=2.0.0
tensorflow>=2.13.0
deepod>=0.4              # Deep Isolation Forest (Wave 2)

# ── NLP Embeddings ──────────────────────────────────────────────
sentence-transformers>=2.2.0

# ── Agent Framework ─────────────────────────────────────────────
langgraph>=0.0.26
openai>=1.0.0              # HTTP client for vLLM's OpenAI-compatible API

# ── Data Collection & Integration ────────────────────────────────
kafka-python>=2.0.0
SQLAlchemy>=2.0.0
requests>=2.31.0

# ── Monitoring & Visualization ──────────────────────────────────
psutil>=5.9.0
matplotlib>=3.7.0
seaborn>=0.13.0

# ── Configuration ───────────────────────────────────────────────
PyYAML>=6.0

# ── Explainability ──────────────────────────────────────────────
shap>=0.45                # Wave 1 SHAP attribution

# ── Drift & Adaptive Retraining ─────────────────────────────────
alibi-detect>=0.11.5      # KSDrift/MMDDrift feature drift (Wave 2)
river>=0.21.2             # ADWIN performance drift (Wave 2)

# ── Secrets Management (optional - for Kubernetes deployments) ──
kubernetes>=24.0.0
